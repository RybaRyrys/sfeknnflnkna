<Page x:Class="itog.ItemTypePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:itog"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ItemTypePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="TypeDgr" SelectionChanged="TypeDgr_SelectionChanged"/>
        <Label Content="Тип" Grid.Row="1"/>
        <TextBox x:Name="TypeTbx" Grid.Row="3" />
        <Button Content="Добавить данные" Grid.Row="4" Click="Add_Click"/>
        <Button Content="Изменить данные" Grid.Row="5" Click="Change_Click"/>
        <Button Content="Удалить данные" Grid.Row="6" Click="Delete_Click"/>
        <Button Content="Импортировать данные" Grid.Row="7" Click="Import_Click"/>
    </Grid>
</Page>
