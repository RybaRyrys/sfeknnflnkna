<Page x:Class="itog.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:itog"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrdersPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="OrderGrd" Grid.Row="0"/>
        <Label Content="id Клиента" Grid.Row="1"/>
        <ComboBox x:Name="ClientBx" Grid.Row="2"/>
        <Label Content="id Работника" Grid.Row="3"/>
        <ComboBox x:Name="WorkerBx" Grid.Row="4"/>
        <Button Content="Добавить заказ" Grid.Row="5" Click="Add_Click"/>
        <Button Content="Изменить заказ" Grid.Row="6" Click="Change_Click"/>
        <Button Content="Удалить заказ" Grid.Row="7" Click="Delete_Click"/>
    </Grid>
</Page>
